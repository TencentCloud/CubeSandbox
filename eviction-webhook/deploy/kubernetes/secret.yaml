apiVersion: v1
kind: Secret
metadata:
  name: eviction-webhook-auth
  namespace: cube-system
type: Opaque
stringData:
  # Replace these placeholders before deploying. The values must match
  # CubeMaster's HMAC auth configuration.
  user_id: "eviction-webhook"
  secret_key: "CHANGE_ME"

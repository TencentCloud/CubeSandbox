  4 个子 agent 并行审查了路由/中间件层、cube handlers、行为回归/安全、meta/notify/inner+测试。结论：路由注册层是真的用上了 gin（这点确实修复了），但业务

  handler 内部大量残留"伪 gin"模式，改造不彻底、包与包之间水平不一致。

  结论先说：套壳评分 ~4/10，未完全达成"真正用上 gin"

  ---

  🔴 核心遗留问题（Major）

  1. cube 包的 handler 仍是"一个 handler + 手写 switch c.Request.Method"

  routes.go 里把多方法注册到同一函数，handler 内部再 switch 分发，而不是像 gin 惯用法那样为每个方法单独注册 handler：

  - snapshot.go:134 handleSnapshotAction（GET/POST/DELETE 共用）

  - template.go:68、template_compat.go:39、template_from_image.go:29

  - sandboxutil.go:20、image.go:32/49

  这正是用户最初吐槽的"没用上 gin 能力"的核心症状——routes.go 已证明作者会用 g.GET/[g.POST](http://g.POST) 分别注册（HEAD/GET 复用等场景是合理的），但对语义完全不同的

  POST/GET/DELETE 仍选择偷懒共用一个函数体。对比：meta/notify 包完全没有这个问题，同一次改造下两个包水平不一致。

  2. 大量业务函数仍是纯 net/http 签名，只在最外层包一层 gin.Context

  createSnapshot(r *http.Request, rt* RequestTrace)、getSnapshot(...)、deleteSnapshot(...)、createTemplate、getTemplate、listTemplates、createTemplateFromImage

  、previewSandbox、createSandbox、deleteSandbox 等——这些是典型的"参数从 (w,r) 换成 (c *gin.Context)，内部 100% 保留原 net/http 代码"的表面重构。

  3. Query 解析几乎全部绕开了 c.Query()，仍手写 c.Request.URL.Query() / r.URL.Query()：snapshot.go:155-156/367-372/394/464、template.go:214-215、template_from_

  image.go:124/158-159/195、list.go:40-63、info.go:30-38、inner/routes.go:36-38。行为一致但确实没用上 gin 封装，属于无痛可替换项。

  ---

  🟡 值得留意但可接受的取舍（Minor）

  - Body 解析绕开 c.ShouldBindJSON，走自定义 common.GetBodyReq/utils.DecodeHttpBody（基于 jsoniter）。这是为了保留 FastestJsoniter

  序列化和大数字精度，是合理取舍，但两套解析函数并存（GetBodyReq vs DecodeHttpBody）是历史债务，没有借这次机会统一。

  - gin_response.go 完全不用 c.JSON，直接 WriteResponse(c.Writer, ...)。合理（避免 gin 默认 encoding/json），但更规范的做法是实现 gin.Render 接口配合

  c.Render()。gin_response_test.go 确认这是刻意锁定 Content-Type: application/json（无 charset）契约，测试有实质价值。

  - panic recovery 用裸 recover() 而非 gin.Recovery()：与旧 mux 版本语义等价迁移，非本次引入的回归，可选优化用 CustomRecoveryWithWriter。

  - inner 包的 websocket/query handler 仍是 (w http.ResponseWriter, r *http.Request) 签名转发——可接受，因为 websocket upgrade 依赖标准库签名，用 gin.Context

  无实质收益。

  ---

  ✅ 已验证无安全回归

  - 认证绕过：未发现。四个 RegisterXRoutes 都在 root.Use(middleware.GinRequestMiddleware()) 之后注册，gin 的 Group() 正确继承了父组中间件。NoRoute/NoMethod

  不挂中间件是 fail-closed（不读取任何受保护数据），且已用源码验证 gorilla/mux 本来就是这个行为，不是本次杜撰的 parity。

  - panic recovery 语义：[c.Next](http://c.Next)() 是普通调用栈递归，下游 panic 能被 GinRequestMiddleware 里的 defer recover() 捕获，与旧版等价（路由匹配阶段的 panic

  两者都不保护，新旧一致，非回归）。

  - 路径穿越：ca_download.go 用 c.Param("filename") 替代手动 filepath.Clean，gin path param 天然不含 /，防护更简洁且未退化。

  - 并发安全：未发现 goroutine 直接捕获 gin.Context 而缺 c.Copy() 的模式。

  🟢 一个真实的小差异（Low，非本次刻意但值得记录）

  gin 在 405 分支会自动设置 Allow header（列出该路径支持的方法），而旧 gorilla/mux 的 405 处理从不设置 Allow header。server_test.go:66-71 只断言状态码和空

  body，没覆盖这个 header 差异——与 PR 声称的"完全 mux

  parity"不完全相符，风险极低（只是暴露支持的方法集合），建议在测试里显式记录这个已知差异（是保留还是要抑制）。

  ---

  建议的下一步（按优先级）

  1. 拆分 cube 包里的多方法共用 handler：把 handleSnapshotAction 等按方法拆成独立函数分别注册到 g.GET/g.POST/g.DELETE，消除内部

  switch，这是用户最初诉求的核心，目前只有 meta/notify 包做到了。

  2. 把散落的 r.URL.Query() 替换成 c.Query()/c.DefaultQuery()（低风险、纯净化）。

  3. 补一条测试或注释说明 405 的 Allow header 差异是有意保留。

  4. 视时间成本决定是否值得把 createSnapshot(r *http.Request, ...) 这类函数改造为直接接收

  *gin.Context（收益有限，主要是风格一致性，非功能性问题，可以作为后续小步重构，不必阻塞当前 PR）。
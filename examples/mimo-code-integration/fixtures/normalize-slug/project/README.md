# Normalize Slug Demonstration Fixture

Implement `normalize_slug` in `app.py`. The fixed tests are the acceptance
contract for this reference-pattern demonstration; the source reruns the
original baseline tests before promotion can succeed.

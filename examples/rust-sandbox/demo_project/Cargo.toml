[package]
name = "rust-demo"
version = "0.1.0"
edition = "2024"
description = "Minimal axum HTTP server for CubeSandbox Rust template warm-up"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

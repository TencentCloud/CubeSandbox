# Restricted toolbox workspace (inside the BYOI image).
#
# Prefer: cube-tool toolbox-hello
#   → writes /workspace/out/hello.txt and prints WORKLOAD_OK
#
# Guest re-checks /etc/cube-sandbox/tool-profile.txt via cube-tool.

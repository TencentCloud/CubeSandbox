# Pydantic AI agent framework — requires Python 3.10+.
# Floor is the version this example was validated against; it relies on
# Agent(instructions=), run_sync(..., model=), result.output, and the
# OpenAIChatModel + OpenAIProvider pair, which are not guaranteed on older 2.x.
pydantic-ai>=2.46.0,<3.0
# Official CubeSandbox Python SDK (from cubesandbox import Sandbox).
# >=0.7.0 for the commands/files envd API and the allow_internet_access flag.
cubesandbox>=0.7.0
# Loads the local .env file.
python-dotenv>=1.0
